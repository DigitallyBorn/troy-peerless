<%= simple_form_for @announcement do |f| %>
<%= f.input :title %>
<%= f.input :publish_date, as: :date, html5: true %>
<%= f.input :body %>
<%= f.button :submit, class: 'btn btn-primary pull-right' do %>
<i class="fa fa-save"></i>
<% end %>
<% end %>
